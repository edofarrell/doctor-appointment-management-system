<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/dp_16px"
        android:paddingHorizontal="@dimen/dp_24px"
        android:paddingBottom="@dimen/dp_24px"
        >

        <TextView
            android:id="@+id/tv_daftar_dokter"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"

            android:text="@string/doctor_list"
            android:textStyle="bold"
            android:textColor="@color/black"
            android:textAlignment="center"
            android:textSize="@dimen/h5"
            />

        <ListView
            android:id="@+id/lst_foods"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tv_daftar_dokter"
            android:layout_marginTop="@dimen/dp_16px"
            android:divider="@android:color/transparent"
            android:dividerHeight="@dimen/dp_8px"
            />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btn_add_doctor"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:layout_marginBottom="@dimen/button_margin"
            android:backgroundTint="@color/primaryColor"
            android:src="@drawable/ic_baseline_add_24_1" />

    </RelativeLayout>

</FrameLayout>